# 总管优化复盘：妥协与降级清单

> 对本轮（方法论 P0–P2 + 复盘三项 + 体验 backlog #1–#9 + 卡片视觉）所有改动的**诚实自审**：
> 哪些是有意的 v1 范围收窄、哪些靠模型遵从而非代码强制、哪些是真实降级/可能回归、哪些是测试覆盖缺口。
> 目的：让妥协**显式可见**，便于后续按需补强或拍板。分支 `feat/orchestrator-centric`。

---

## A. 有意的 v1 范围收窄（已与用户确认，非偷工）

| 项 | 收窄点 | 留待 |
|----|--------|------|
| #1 inline-edit | 仅改 prompt / 换员工，不增删任务 | v2（增删需重排 depends_on 位置） |
| #2 进化看得见 | 仅被动角标，总管不主动提一句 | v2（触达需克制，待时机设计） |
| #7 失败恢复 | 仅「重试」入口，失败原因不内联 | 失败原因 plumbing |
| #9 团队交付物 | 仅 write_file/edit_file 事件归属 | shell 直接写的文件（用户明确先不管） |
| #4 信任档位 / #5 现场感 | 整体缓做 | 按需触发（见 backlog） |

---

## B. 靠模型遵从、非代码强制（最该警惕——模型不照做即失效）

- **P0-A 质检抽检**：纯提示词要求总管对高风险交付 ls+read 核实；模型可不照做。
  → 仅 **#3 qa_delivery_check** 对「员工自报的二进制交付物」做了代码兜底，其余靠遵从。
- **P2-B 产物撞名**：靠派活契约提示词「并行任务用不同文件名」+ 仅写一条日志告警，**不 prevent** 覆盖。
- **#9 distinct-naming**：同上，靠提示词。
- **决策链「不得跳步」、派活契约四要素完整、只对用户说人话**：均纯提示词，无机械校验。

---

## C. 真实降级 / 可能回归（建议拍板）

1. ⚠️ **多计划会话只显示最新计划的交付物**（#9 + 位置调整引入）。
   交付物卡从「每张计划卡内」移到「本轮最后消息末尾页脚」，并**从计划卡移除**后：一个会话里有多个已完成编排计划时，**早先计划的交付物不再在任何地方展示**（页脚 `CuratorTurnDeliverables` 只取最新非 pending/cancelled 计划）。
   - 单计划会话（最常见）无影响；多计划长会话是降级。
   - vs 本轮之前（根本没有交付物展示）仍是净增；vs 中间版本（每计划卡各显）是回退。
   - 可选补强：页脚展示全部已完成计划的交付物，或计划卡内也保留一份。

2. ⚠️ **edited 文件的 size 用 new_string 长度**（#9 + 现有 FilesChangeCard 口径）。
   交付物/文件卡对「已编辑」文件显示的「N 字符」是**本次编辑片段长度**，非文件实际大小，在「成果视角」下易误导。created（write_file）取 content 长度则准确。

3. **P2-A 提示词瘦身**：删掉了「禁止复述自动放行/DAG/reported_at…」的**枚举**。
   若模型偶发泄漏内部机制黑话，是轻微回归（概率低；功能性指引全保留）。

4. **P1-A 低风险单任务自动执行**：「只读」判定是**关键词启发式**；被派员工 `enable_hitl=False`，一旦误判把含破坏性操作的任务当只读自动放行，则无 HITL 拦截。
   - 保守口径（small 档 + 无 cron + 破坏性词命中即走确认）已尽量压低，但非零风险。

---

## D. 测试覆盖妥协

- **所有前端 UI 改动无组件/渲染测试**，仅 `tsc --noEmit` + 人工验收：
  PlanEditDialog、PlanDeliverablesCard、CuratorTurnDeliverables、技能候选角标、TaskReworkButton、团队名片、计划卡去重/视觉、HTML 预览按钮。
  → 后端逻辑均有 TDD（≈30+ 新测试）；**前端靠类型 + 手测**，渲染条件/交互无自动化回归。
- **rework 端点测试** monkeypatch 了 `redispatch_task_in_session`，未端到端验证真实返工流（起员工流需 main loop）。
- 已知预存失败已修 5 条（GBK 断言 / 并发闸默认翻转），当前后端 730 passed、0 failed。

---

## E. 已知小限制（零碎）

- **#2 promote_skills**：候选已存在时仍先调一次 LLM 才在 no-clobber 处跳过（白调，受 5min 限流约束）；近义 slug 可能产出多个近重复候选，靠人复审去重。
- **#1 画像消费教训**：librarian 读 `memories/AGENTS.md` 截断**末 2000 字**（长记忆早段被截）。
- **#9 存在性过滤**：相对路径在产物目录解析失败时**保守保留**——极端情况下可能残留一个已删文件的死链（宁可多显不误删）。
- **#3/#7 一键返修/重试**：复用 redispatch，**用户主动发起的返修也受「每任务自动返工 2 次」上限约束**——高频手动改可能被「已达上限」挡住（设计如此，但对"用户主动"略反直觉）。

---

## F. 流程妥协（透明记录）

- 多数功能走的是**精简流程**（设计 inline 评审 → TDD → 提交），**未写正式 spec 文档、未跑 spec-reviewer 子代理、未走 writing-plans**——此为本季早期与用户约定的节奏。
- 仅 **#2 硬技能晋升**做了独立 code-review 子代理审查；其余主要自审 + TDD + tsc 兜底。
- 安全敏感的 **#4** 主动缓做、不自主放宽安全门——是审慎而非妥协。

---

## 优先建议（若要补强）

1. **C-1 多计划交付物丢失** 与 **D 前端测试缺口** 是最值得补的两项（前者是功能降级，后者是质量保障缺口）。
2. **C-2 edit size** 一行可修（edited 不显示 size 或标注"片段"）。
3. B 类（靠模型遵从）若要更稳，方向是把更多校验下沉代码（如 #3 那样）。
